# student_manager
Simple Python project for managing students with SQLite database.
# Student Manager

## 📌 Project Description
A simple Python project for managing student data using **SQLite** database.  
It allows adding students, validating their data (age, email), and storing them safely in the database.

---

## 🚀 How to Run
1. Make sure you have Python installed (version 3.8 or higher).
2. Clone the repository:
   ```bash
   git clone https://github.com/tahamohmadf19-dev/student_manager.git
   cd student_manager

python main.py

